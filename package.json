{
  "name": "kanban-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n worker,server,web -c blue,yellow,green \"pnpm --filter @kanban/worker dev\" \"pnpm --filter @kanban/server dev\" \"pnpm --filter @kanban/web dev\"",
    "build": "pnpm --filter @kanban/worker build && pnpm --filter @kanban/server build && pnpm --filter @kanban/web build",
    "lint": "pnpm --filter @kanban/web lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
